#!/bin/bash

#CONFIG_FILE="config.json"
#script_name=$(basename "$0" .sh) 

# Leer la configuración actual
#value=$(jq -r ".${script_name}.value" "$CONFIG_FILE")

#if [[ "$value" == "enabled" ]]; then
#else
#fi

echo "Onscreen Keyboard configuration here"